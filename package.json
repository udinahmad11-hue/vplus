{
  "name": "visionplus-headers",
  "version": "1.0.0",
  "description": "VisionPlus Headers Capture",
  "main": "api/capture.js",
  "scripts": {
    "vercel-build": "npm install && npx playwright install chromium"
  },
  "dependencies": {
    "@sparticuz/chromium": "^119.0.0",
    "playwright-core": "^1.40.0"
  }
}
