{
  "name": "visionplus-headers",
  "version": "1.0.0",
  "description": "VisionPlus Headers Capture",
  "main": "api/capture.js",
  "scripts": {
    "vercel-build": "npm install playwright"
  },
  "dependencies": {
    "playwright": "^1.40.0"
  }
}
